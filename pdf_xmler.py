#!/usr/bin/python
#-*-coding:utf-8-*-
#Author:Citron
'''
       Used to save objects generated by parsing XML files
'''
#single char text
class pdf_xmler:
     def __init__(self,point,char,size):
         self.point=point
         self.size=size
         self.char=char


#single line text
class pdf_list(list):
    def __init__(self,line_point):
         list.__init__([])
         self.line_point=line_point



#struct point
class point:
    def __init__(self,top,bottom,left,right):
        self.top=top
        self.bottom=bottom
        self.left=left


class block:
     def __init__(self,line_list):
         self.block_list=[]
         self.line_list=line_list

     def split_block(self):
        block=pdf_list(line_list.point)
        for index in range(1,len(line_list)):
           if(line[index].point.left-line_list[index-1].point.right<line_list[index].size):
                  block.append(line_list[index])
           else:
                  self.block_list.append(block)
                  block=pdf_list(line_list.point)
